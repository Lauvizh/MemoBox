{% extends "LFMemoBoxBundle::layout.html.twig" %}

{% block content%}
<div class="row">
	<div class="col-xs-12">
		<p>block content memobox</p>
		{% for theme in themesHomePage %}
			<div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
				<div class="well">
			      	<h2>{{ theme.name }}</h2>
			      	<ul>
			      		{% for event in theme.getEvents() %}
			      			<li>{{ event.title }}</li>
			      		{% endfor %}
			      	</ul>
			    </div>
			</div>
		{% endfor %}
	</div>
</div>
{% endblock %}
